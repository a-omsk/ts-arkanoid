{
  "name": "arkanoid",
  "version": "1.0.0",
  "description": "basic typescript arkanoid implementation",
  "scripts": {
    "compile": "webpack --config webpack.config.js --mode production",
    "compile:watch": "webpack --config webpack.config.js --watch --mode development",
    "serve": "http-server dist -p 3000",
    "start": "npm run compile && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.11.1",
    "ts-loader": "^4.1.0",
    "typescript": "^2.7.2",
    "webpack": "^4.3.0",
    "webpack-cli": "^2.0.13"
  }
}
